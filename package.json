{
  "name": "orbit-workspace",
  "version": "0.7.1-beta",
  "private": true,
  "description": "Orbit P2P Chat — Workspace Root",
  "scripts": {
    "start": "cd desktop && electron .",
    "desktop:start": "cd desktop && electron .",
    "desktop:build:win": "cd desktop && electron-builder --win",
    "desktop:build:mac": "cd desktop && electron-builder --mac",
    "desktop:build:linux": "cd desktop && electron-builder --linux",
    "mobile:sync": "cd mobile && npm run shared:sync && npm run version:sync && npx cap sync android",
    "mobile:build": "cd mobile && powershell -File build-android.ps1",
    "mobile:open": "cd mobile && npx cap open android",
    "test:unit": "node tests/unit/qr-pairing.test.js && node tests/unit/e2ee-key.test.js && node tests/unit/e2ee-interop.test.js && node tests/unit/update-check.test.js && node tests/unit/audio-cover.test.js && node tests/unit/transfer-state-machine.test.js"
  }
}
